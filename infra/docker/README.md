# infra/docker

Implemented in Session 13. Will contain:

- `apps/web/Dockerfile`, `apps/api/Dockerfile`, `workers/Dockerfile`
- `docker-compose.production.yml` with services: web, api, worker, postgres, redis, minio, caddy, plus optional backup/monitoring.

Do not add real images or compose files here until Session 13 — keeping this a documented placeholder avoids temporary architecture.
