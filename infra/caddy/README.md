# infra/caddy

Implemented in Session 13. Will contain the `Caddyfile` providing:

- Automatic HTTPS for the web and api domains
- Reverse proxy to the `web` and `api` services
- Secure headers and upload size limits
